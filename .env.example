# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=school_pilot

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
# IMPORTANT: Change JWT_SECRET to a strong random value in production
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h

# CORS Configuration (optional - defaults to allow all origins)
CORS_ORIGIN=*

# SSL Configuration for Production Database (optional)
DB_SSL=false
# DB_SSL_CA=/path/to/ca-certificate.pem

# Other environment variables as needed
